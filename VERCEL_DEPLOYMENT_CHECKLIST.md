# Goal: Connect the TRL Evidence Planner repository to Vercel

Starting point: The public GitHub repository `akeelm123/trl-evidence-planner` exists, but GitHub currently reports that it is empty. The local calculator is already configured as a Vite Node.js project with `package.json`, `package-lock.json` and `vercel.json`.

- [x] 1. Push the calculator code to the GitHub `main` branch.
- [ ] 2. Sign in to Vercel with GitHub and authorise repository access.
- [ ] 3. Import `akeelm123/trl-evidence-planner` as a Vercel project.
- [ ] 4. Confirm the Vite build and output settings.
- [ ] 5. Deploy and verify the public `.vercel.app` address.
- [ ] 6. Confirm that future pushes trigger automatic deployments.

Notes:

- Step 1: Verified that local `main` tracks `origin/main` at commit `e9fe198`.
